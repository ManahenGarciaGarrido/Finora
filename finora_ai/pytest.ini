[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=. --cov-report=xml:coverage.xml --cov-report=term-missing

[coverage:run]
omit =
    tests/*
    */__pycache__/*
    .venv/*
    train_model.py
source = app
